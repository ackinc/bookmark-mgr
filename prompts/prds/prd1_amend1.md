# PRD 1 Amendment 1

Get rid of the pannable canvas. Just present the bookmarks as a nested list.

Display the keywords associated with each bookmark next to them.

Use the names of the folders a bookmark is in (except the browser default folders - "bookmarks bar", etc.) for keyword extraction.
