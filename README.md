# Bookmark Manager

## Features

- two-way sync with browser's bookmarks
- graph view with semantic clustering
- semantic search
- info extraction from bookmarks (for better clustering)

## Working on bookmark-mgr

- Ensure node and pnpm are installed
- Run `pnpm install`
- Run `pnpm dev`